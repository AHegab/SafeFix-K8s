apiVersion: v1
kind: Pod
metadata:
  name: secure-pod-readonly-rootfs
  namespace: production
spec:
  containers:
  - name: app
    image: nginx@sha256:abcd1234
    securityContext:
      readOnlyRootFilesystem: true
