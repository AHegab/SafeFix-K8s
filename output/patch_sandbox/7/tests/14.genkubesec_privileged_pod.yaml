apiVersion: v1
kind: Pod
metadata:
  name: pod-name
  annotations:
    container.apparmor.security.beta.kubernetes.io/some-container: runtime/default
spec:
  containers:
  - name: some-container
    image: some-image
    command:
    - some-command
    securityContext:
      privileged: true